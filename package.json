{
  "name": "kakezan-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test",
    "check": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

